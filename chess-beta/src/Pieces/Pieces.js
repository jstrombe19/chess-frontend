import React from 'react';

export default class Pieces, extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
      position: null
    };
  }

  render() {
    return(

    )
  }
}
